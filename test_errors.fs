# This should cause a syntax error (missing semicolon)
x = 5

# This should cause an unknown character error
@

# This should cause an undefined function error
چاپ جمع(1, 2);

# This should cause an unknown operator (if you try something like x $ 3)
