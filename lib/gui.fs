// کتابخانهٔ رابط کاربری (GUI)

تابع پنجره(عنوان) {
    اجرا("python3 lib/gui_helper.py window " + عنوان);
}

تابع برچسب(متن) {
    اجرا("python3 lib/gui_helper.py label " + متن);
}

تابع دکمه(متن) {
    اجرا("python3 lib/gui_helper.py button " + متن);
}
