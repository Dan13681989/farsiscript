[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "farsiscript-lang"
version = "0.2.5"
description = "A simple programming language with Farsi syntax that compiles to C"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Your Name", email = "you@example.com"}
]
keywords = ["farsi", "compiler", "language"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Compilers",
]

requires-python = ">=3.8"
dependencies = []

[project.scripts]
fs = "farsiscript.main:main"

[project.urls]
Homepage = "https://github.com/Dan13681989/farsiscript"
Repository = "https://github.com/Dan13681989/farsiscript.git"
Issues = "https://github.com/Dan13681989/farsiscript/issues"
