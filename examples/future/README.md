# Future expansions

- **LSP Server**: use `pygls` library to provide autocomplete & diagnostics for VS Code.
- **Discord Bot**: run `farsiscript` code inside a `discord.py` bot.
- **Mobile**: package with `briefcase` from BeeWare.
